rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for development - replace with proper rules later
    match /{document=**} {
      allow read, write: if true;
    }
  }
}